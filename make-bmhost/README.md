This is temporary.  We can use the `make-bm-worker` utility from the
`baremetal-operator` repository once it has the `boot-mac` option merged.

See https://github.com/metal3-io/baremetal-operator/pull/174
