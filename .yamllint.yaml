yaml-files:
- '*.yaml'
- '*.yml'
- '.yamllint'

ignore: |
  tests/roles/run_tests/templates/

rules:
  trailing-spaces: enable
  key-duplicates: enable
  indentation:
    spaces: 2
    indent-sequences: false # Enforce k8s-style indentation
    check-multi-line-strings: false
  truthy:
    allowed-values: [ 'true', 'false', 'yes', 'no', 'on' ]
